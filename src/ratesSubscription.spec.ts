import React from 'react';

describe('ratesSubscription', () => {
  test('can subscribe to rates and make a get request', () => {});

  test('can call callback with response data ', () => {});

  test('when subscribed can make requests every ten seconds', () => {});

  test('can unsubscribe and stop making requests', () => {});
});
